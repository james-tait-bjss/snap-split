{
  "name": "snap-split",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "start": "rm -rf dist/* && tsc && node dist/src/server.js --watch",
    "lint": "eslint . --ext .ts",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.11",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "coverageReporters": ["lcov", "text-summary"],
    "testPathIgnorePatterns": ["dist/*"]
  }
}
